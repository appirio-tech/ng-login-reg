header
  .flex.middle
    h4 account info

    p Username and email are not editable.

  hr

main
  h6 username

  p= username

  h6.email-address email address

  p= email

  hr

  form(onSubmit=onSubmit)
    h6 password

    p Current Password

    input(type="password" placeholder="Enter Password" onChange=currentPasswordChange)

    p New Password

    input(type="password" placeholder="Enter Password" onChange=newPasswordChange)

    button.action(disabled=saveDisabled) save