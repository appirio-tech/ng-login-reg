layout-header.elevated(role="banner")

main.layout-main.login.flex.column.center("role"="main" layout-main=true flush-height=true)
  h1 reset password

  p.subheader.secondary Let's start building your app.  Login or create an account below to begin.

  p.error(ng-show="vm.error") Enter your email and we will send you a link to reset your password.


  form(ng-submit="vm.submit()" method="post")
    input.wide(type="text" ng-model="vm.email" required=true placeholder="email")

    button.action(type="submit") reset

    p.reminder Don't have an account? <a ui-sref="register">Sign Up</a>

layout-footer