p.success Your password has been updated.

form(ng-submit="vm.submit()" method="post")
  input.wide(type="password" ng-model="vm.email" required=true placeholder="New Password")

  button.action(type="submit") reset password

